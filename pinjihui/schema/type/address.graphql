type Address {
    # 省份ID
    provinceId: Int!
    # 城市ID
    cityId: Int!
    # 区县ID
    areaId: Int!
    # 地区全名, 如 四川 成都市 高新区
    regionName: String
    # 区县后的详细地址, 如 xx街xx号
    address: String!
}

# 地址输入
input AddressInput {
    areaId: Int!
    # 区县后的详细地址, 如 xx街xx号
    address: String!
}