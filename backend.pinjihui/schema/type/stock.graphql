type Stock {
    productId: ID!
    merchantId: ID!
    stock: Int!
    retailprice: Float!
    createdAt: Time!
    salesVolume: Int!
}

input StockSearchInput{
    productId: ID
    merchantId: ID
}